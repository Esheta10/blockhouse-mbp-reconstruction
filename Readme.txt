# Blockhouse Quantitative Developer Trial

## 📌 Task Summary
Reconstruct the MBP-10 (Market by Price) order book using MBO (Market by Order) data from `mbo.csv`, and generate an output file `output_mbp.csv` in the same format as `mbp.csv`.

## ⚙️ How to Build and Run
Open terminal in the project directory and run:

